1) Загрузить JSON файл - отдельный сервис по запросу отдающий карточку клиента
Реализовать переключатели-фильтры по фильмам. - отдельный сервис фильтрующий по тем или иным параметрам

2) При помощи ajax запросов к загруженному файлу сформировать на странице карточки Героев со всеми данными
(фото, имя, настоящее имя,  список фильмов, статус). 
1 персонаж - 1 карточка.

3) Стилизация карточек и всего внешнего вида - на ваше усмотрение.

*4) Упор сделать на главную цель - донесение информации, никаких вырвиглазных цветов и шрифтов.

Работает как отдельное приложение
1 - Запрашиваем файл с данными - делаем хелпер
1.1. - выполняем 1, потом собираем в отдельный массив по заданному фильтру(главная фильтрация по фильму),
список фильтров составляем из исходного сервиса предоставляющего всю информацию,
далее работаем с тем или иным объектом, через хелпер.
Возращаем список для фильтрации.
1.2. - Получаем всю информацию о Героев и выводим массив объектов + фильтрацию
1.3. - Визуализировать через сервис

Работает как отдельное приложение
2) Отдельный сервис визуализации, в карточке информацию выводим в таблице, в списке фильмов, сделать по нажатию на название фильма, фильтрацию по фильму.

Сервис который отслеживает работы сервисов
